// 1. How to declare a variable using let and const
const fathername = 'babul'
let season = 'rainy'

// 2. 6 basic Conditions >,<, ===, !==, <=, >=, &&, ||

// 3. Array, index, length, push,
// 4. Loop, for loop, for of, for in, for each
// 5. Function
// 6. object
const object = {
    name: 'Tishu',
    relation: "Abir's love",
}
const variable = 'name';
console.log(object.name); // direct by property
console.log(object['relation']); // access via property name string
console.log(object[variable]); // access via property name in variable.
