// for one parameter 2 types
// type 1
const double = (num) => num * 2;
// type 2 (sudhu parameter er brackets nai)
const five = num => num * 5;

// for 2 parameter
const add = (a, b) => a + b;
console.log(add(1, 2));

// for multi parameter
const addAll = (a, b, c, d) => a + b + c + d;

// for no parameter
const getPi = () => 3.1416;

// multiline arrow function
const doMath = (x, y, z) => {
    const sum = x + y;
    const multi = sum * Z;
    return multi;
}